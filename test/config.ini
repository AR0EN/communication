; Configuration for libcommwrapper unit tests
[comm-wrapper]
; `mingw64_bin_dir` is required only on Windows!
mingw64_bin_dir  = ""
wrapper_lib_path = "../build/wrapper/libcommw.so"
shared_lib_exts  = ["dll", "so"]
